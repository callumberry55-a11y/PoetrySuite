# Google Cloud Platform Configuration
# Copy this to .env and fill in your values

# GCP Project Settings
GCP_PROJECT_ID=your-project-id
GCP_REGION=us-central1
GCP_ZONE=us-central1-a

# VPC Configuration
VPC_NETWORK_NAME=default
VPC_CONNECTOR_NAME=poetry-vpc-connector
VPC_SUBNET_RANGE=10.8.0.0/28

# OpenVPN Settings
OPENVPN_SERVER_IP=your-openvpn-server-ip
OPENVPN_PORT=1194
OPENVPN_PROTOCOL=udp
VPN_SUBNET=10.8.0.0/24

# Cloud Run Settings
CLOUD_RUN_SERVICE_NAME=poetry-suite
CLOUD_RUN_MAX_INSTANCES=10
CLOUD_RUN_MIN_INSTANCES=1
CLOUD_RUN_MEMORY=512Mi
CLOUD_RUN_CPU=1
CLOUD_RUN_TIMEOUT=300

# Deployment Settings
DEPLOY_TYPE=cloudrun  # Options: cloudrun, appengine, compute
VPC_EGRESS=all-traffic  # Options: all-traffic, private-ranges-only

# Application Environment Variables (set these in GCP Console or via gcloud)
# These should NOT be committed to version control
VITE_SUPABASE_URL=your-supabase-url
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key
VITE_FIREBASE_API_KEY=your-firebase-api-key
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-firebase-project
VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your-sender-id
VITE_FIREBASE_APP_ID=your-app-id
VITE_FIREBASE_MEASUREMENT_ID=your-measurement-id

# Custom Domain (optional)
CUSTOM_DOMAIN=yourdomain.com
SSL_CERTIFICATE_NAME=poetry-suite-ssl

# Monitoring & Logging
ENABLE_CLOUD_LOGGING=true
ENABLE_CLOUD_MONITORING=true
LOG_LEVEL=info

# Security
ENABLE_CLOUD_ARMOR=false
CLOUD_ARMOR_POLICY_NAME=poetry-suite-policy
ENABLE_IAP=false  # Identity-Aware Proxy
